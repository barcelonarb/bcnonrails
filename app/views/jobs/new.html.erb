<% title 'New job' %>
<h2><%= yield(:title) %></h2>

<% form_for(@job) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => "form", :object => f %>
  
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', jobs_path %>